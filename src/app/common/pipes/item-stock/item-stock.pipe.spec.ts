import { ItemStockPipe } from './item-stock.pipe';

describe('ItemStockPipe', () => {
  it('create an instance', () => {
    const pipe = new ItemStockPipe();
    expect(pipe).toBeTruthy();
  });
});
