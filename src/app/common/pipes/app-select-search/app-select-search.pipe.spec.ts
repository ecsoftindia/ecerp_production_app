import { AppSelectSearchPipe } from './app-select-search.pipe';

describe('AppSelectSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new AppSelectSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
