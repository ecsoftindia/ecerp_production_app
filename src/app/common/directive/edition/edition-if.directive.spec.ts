import { EditionIfDirective } from './edition-if.directive';

describe('EditionIfDirective', () => {
  it('should create an instance', () => {
    const directive = new EditionIfDirective();
    expect(directive).toBeTruthy();
  });
});
