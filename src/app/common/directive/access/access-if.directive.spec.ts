import { AccessIfDirective } from './access-if.directive';

describe('AccessIfDirective', () => {
  it('should create an instance', () => {
    const directive = new AccessIfDirective();
    expect(directive).toBeTruthy();
  });
});
