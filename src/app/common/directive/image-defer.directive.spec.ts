import { ImageDeferDirective } from './image-defer.directive';

describe('ImageDeferDirective', () => {
  it('should create an instance', () => {
    const directive = new ImageDeferDirective();
    expect(directive).toBeTruthy();
  });
});
